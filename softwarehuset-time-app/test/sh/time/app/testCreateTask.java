package sh.time.app;
import static org.junit.Assert.assertEquals; 
import static org.junit.Assert.assertFalse; 
import static org.junit.Assert.assertTrue; 
import static org.junit.Assert.fail; 

import java.util.List; 

import org.junit.Test; 

public class testCreateTask {
/*	
	@Test
	public void testLogIn() {
		TimeApp timeApp = new TimeApp(); 
		
		//check developer is not logged in
		assertFalse(timeApp.developerLoggedIn()); 
		
		// wrong developer login
		boolean login = timeApp.logIn("wrong"); 
		
		//check developer logged in
		assertFalse(login);
		assertFalse(timeApp.developerLoggedIn()); 
				
		// correct developer login
		login = timeApp.logIn("devdev"); 
		
		//check developer logged in
		assertTrue(login);
		assertTrue(timeApp.developerLoggedIn()); 
	}
	
*/
	
}
